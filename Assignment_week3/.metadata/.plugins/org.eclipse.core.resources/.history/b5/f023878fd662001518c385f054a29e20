package solotion01;

public class Soloution01 {
	public void sortedMerge(int[] des, int[] src, int length) {
		int size = des.length;
		int index = src.length;
		while(index>=0) {
			if(src[index-1] > des[length-1]) {
				des[size-1] = src[index]
			}
		}
	}
}
