package solution;

import java.util.LinkedList;

/**
 * This class file is to design an algorithm to get the rank of a given number 
 * in an array.
 * @author qiuyi
 *
 */
public class Solution10 {
	private static LinkedList<Integer> arrayList = new LinkedList<Integer>();
	
	public static void track(int x) {
		if(List.isEmpty()) arrayList.add(x);
	}
}
